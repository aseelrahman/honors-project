# Add this at the end of your Rakefile
namespace :assets do
  desc "Stub for assets:precompile task"
  task :precompile do
    puts "Using Propshaft for assets - precompilation handled differently"
  end
end